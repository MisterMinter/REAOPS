export default function SettingsPage() {
  return (
    <div>
      <h1 className="font-display text-3xl text-[var(--txt)]">Settings</h1>
      <p className="mt-2 max-w-2xl text-[var(--txt2)]">
        Connect HubSpot, Buffer, and configure your Drive folder from here once OAuth routes are live.
      </p>
      <div className="mt-10 rounded-lg border border-[var(--border)] bg-[var(--card)] p-8 text-[var(--txt3)]">
        Integration controls coming in Phase 2–6.
      </div>
    </div>
  );
}
