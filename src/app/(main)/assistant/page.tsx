export default function AssistantPage() {
  return (
    <div>
      <h1 className="font-display text-3xl text-[var(--txt)]">Broker Assistant</h1>
      <p className="mt-2 max-w-2xl text-[var(--txt2)]">
        Chat with context from your cached HubSpot data will live here after the assistant workflow is
        connected.
      </p>
      <div className="mt-10 rounded-lg border border-[var(--border)] bg-[var(--card)] p-8 text-center text-[var(--txt3)]">
        Chat UI coming in Phase 5.
      </div>
    </div>
  );
}
