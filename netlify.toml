[build]
  publish = "."

# Edge Function configuration (preferred for streaming support)
[[edge_functions]]
  function = "anthropic-proxy"
  path = "/api/anthropic"

# Regular function as fallback (if Edge Functions aren't available)
# The function will be available at /.netlify/functions/anthropic-proxy
# But we'll use the Edge Function path above for better performance
